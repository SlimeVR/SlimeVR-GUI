{
  "name": "slimevr-ui",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "gui": "cd gui && yarn run tauri dev",
    "build": "cd gui && yarn run tauri build",
    "tauri": "cd gui && yarn run tauri",
    "preinstall": "cd gui && yarn install",
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "^8.0.2"
  }
}
