{
  "name": "slimevr-ui",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "start": "cd gui && cross-env BROWSER=none node scripts/start.js",
    "gui": "cd gui && yarn run tauri dev",
    "tauri": "cd gui && yarn run tauri",
    "preinstall": "cd gui && yarn install --frozen-lockfile",
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "^8.0.2"
  }
}